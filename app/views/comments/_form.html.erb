<%= form_for([@post, @post.comments.build], remote: true) do |f| %>
  <div class="field">
    <%= f.label :commenter %><br>
    <%= f.text_area :commenter, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>